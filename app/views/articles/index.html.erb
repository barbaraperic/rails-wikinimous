

<div class="container mt-4">
<% @articles.each do |a| %>
<div class="card p-4">

    <h2><%= a.title %></h2>
    <p><%= a.content %></p>
    <%= link_to 'edit', edit_article_path(a), class: "btn btn-outline-dark mr-auto"%>
</div>
<% end  %>

</div>
